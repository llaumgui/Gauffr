{use $gauffrUsers, $gauffrSlave, $installRoot}
{tr_context "view/full/user_credential.ezt"}

<div class="block">
    <h2>{tr "Users"}</h2>

    {include "view/list/user_credential.ezt"
        send $gauffrUsers as $gauffrUsers,
             $gauffrSlave as $gauffrSlave,
             $installRoot as $installRoot}
</div>